<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hotel_everywhere">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hotel_Everywhere"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ReservationActivity" android:label="ReservationActivity"></activity>
        <activity android:name=".MyPageActivity" android:label="MyPageActivity"></activity>
        <activity android:name=".RoomServiceActivity" android:label="RoomServiceActivity"></activity>
        <activity android:name=".MeetingRoomServiceActivity" android:label="MeetingRoomServiceActivity"></activity>
        <activity android:name=".RestaurantActivity" android:label="RestaurantActivity"></activity>
    </application>

</manifest>